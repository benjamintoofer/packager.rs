
use super::ManifestType;
// Responsible for generating manifests and obscuring which manifset it is generating
pub struct ManifestController {

}

impl ManifestController {
  pub fn generate_manifest(manifest_type: ManifestType) -> String {

    "".to_string()
  }
}